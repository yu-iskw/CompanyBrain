{
  "name": "@companybrain/policy",
  "version": "0.1.0",
  "private": true,
  "description": "Policy engine that may restrict access but never expand it.",
  "license": "Apache-2.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "dependencies": {
    "@companybrain/authorization": "workspace:*",
    "@companybrain/domain": "workspace:*"
  }
}
